backend:
  name: git-gateway
site_url: https://jeangelique.com
display_url: https://jeangelique.com/surprise-surprise
publish_mode: editorial_workflow
media_folder: static/images
public_folder: /images
slug:
  encoding: ascii
  clean_accents: true

collections:
  - label: Souvenirs
    label_singular: Souvenir
    name: souvenirs
    summary: '{{author}} · {{date}} · {{title}}'
    create: true
    delete: false
    folder: src/pages/souvenirs
    # slug: '{{year}}-{{month}}-{{slug}}'
    preview_path: souvenirs/{{slug}}
    fields:
      - label: Author (you)
        name: author
      - label: Punchline (title)
        name: title
      - label: With who?
        name: who
        widget: select
        options: [Angélique, Jens, Jeangelique]
      - label: When?
        name: date
        widget: date
        default: ''
        format: DD MMM YYYY
      - label: Where?
        name: location
        widget: object
        fields:
          - label: City
            name: city
          - label: Country
            name: country
      - label: Picture
        name: image
        widget: image
        required: false
      - label: Story
        name: body
        widget: markdown
        required: false
